/*
 * HealthCheckPage Messages
 *
 * This contains all the text for the HealthCheckPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.HealthCheckPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'The App is Healthy.',
  },
});
